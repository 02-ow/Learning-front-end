// 判断用户名是否符合要求
const username = document.querySelector('.username');
const usernameError = document.querySelector('.error-message');

username.addEventListener('change',function(){
// 定义正则表达式
const re = /^[a-zA-Z]$/;

})
